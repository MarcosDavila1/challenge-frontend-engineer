Este componente recibe por props:
    cartItems: Array
    setCartItems: Function
    show: Boolean
    setShow: Function

Y renderiza el componente CartItem por cada uno de los items que hay en el carrito, si el boolean Show esta en true.